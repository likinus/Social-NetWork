let initialState = {
  bestFriends: [
    { id: 1, name: 'Genya' },
    { id: 2, name: 'Lesha' },
    { id: 3, name: 'Boris' },
  ],
};

const sideBarReducer = (state = initialState, action) => {
  return state;
};
export default sideBarReducer;
